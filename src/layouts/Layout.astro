---
export interface Props {
  title: string;
  description?: string;
  canonical?: string;
  ogImage?: string;
  ogType?: string;
  schema?: object;
}
const {
  title,
  description = 'Personalized handcrafted digital products made just for you.',
  canonical,
  ogImage = 'https://www.personalized-you.com/og-default.jpg',
  ogType = 'website',
  schema,
} = Astro.props;
const canonicalUrl = canonical ?? `https://www.personalized-you.com${Astro.url.pathname}`;
---
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <meta name="generator" content={Astro.generator} />
    <link rel="canonical" href={canonicalUrl} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title} | Personalized You</title>

    <!-- Open Graph -->
    <meta property="og:type" content={ogType} />
    <meta property="og:url" content={canonicalUrl} />
    <meta property="og:title" content={`${title} | Personalized You`} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={ogImage} />
    <meta property="og:site_name" content="Personalized You" />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={`${title} | Personalized You`} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImage} />

    <!-- JSON-LD structured data -->
    {schema && <script type="application/ld+json" set:html={JSON.stringify(schema)} />}
    <style>
      *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
      :root {
        --cream: #fdf8f0;
        --beige: #e8d9c5;
        --sage: #7a9e7e;
        --sage-hover: #5f8463;
        --pink: #e8b4b8;
        --yellow: #f5e6a3;
        --lilac: #c9b8d8;
        --latte: #b07d50;
        --blue: #b8cce4;
        --text: #3a2e28;
        --muted: #7a6a5a;
        --border: #d9cfc4;
      }
      body { font-family: system-ui, -apple-system, sans-serif; background: var(--cream); color: var(--text); min-height: 100vh; display: flex; flex-direction: column; }
      a { color: var(--sage); text-decoration: none; }
      a:hover { text-decoration: underline; }
      nav { background: var(--beige); padding: 0 2rem; display: flex; align-items: center; justify-content: space-between; height: 64px; box-shadow: 0 2px 8px rgba(44,44,44,0.08); }
      .nav-logo { font-size: 1.25rem; font-weight: 800; color: var(--latte); letter-spacing: -0.01em; }
      .nav-logo span { color: var(--latte); }
      .nav-links { display: flex; gap: 1.5rem; list-style: none; }
      .nav-links a { color: var(--text); font-size: 0.95rem; transition: color 0.2s; }
      .nav-links a:hover { color: var(--sage); text-decoration: underline; text-decoration-color: var(--sage); }
      .hamburger { display: none; background: none; border: none; cursor: pointer; color: var(--text); font-size: 1.5rem; }
      main { flex: 1; }
      footer { background: var(--beige); text-align: center; padding: 1.75rem; color: var(--muted); font-size: 0.875rem; border-top: 3px solid var(--latte); }
      footer a { color: var(--muted); }
      footer a:hover { color: var(--sage); }
      @media (max-width: 768px) {
        .hamburger { display: block; }
        .nav-links { display: none; position: absolute; top: 64px; left: 0; right: 0; background: var(--beige); flex-direction: column; padding: 1rem 2rem; gap: 1rem; border-top: 1px solid rgba(44,44,44,0.1); z-index: 100; }
        .nav-links.open { display: flex; }
      }
    </style>
  </head>
  <body>
    <nav>
      <a class="nav-logo" href="/">Personalized You</a>
      <button class="hamburger" id="hamburger" aria-label="Toggle menu">☰</button>
      <ul class="nav-links" id="nav-links">
        <li><a href="/">Home</a></li>
        <li><a href="/bedtime-stories">Bedtime Stories</a></li>
        <li><a href="/baby-album">Baby Album</a></li>
        <li><a href="/blog">Blog</a></li>
        <li><a href="/about">About</a></li>
        <li><a href="/contact">Contact</a></li>
      </ul>
    </nav>
    <main>
      <slot />
    </main>
    <footer>
      <p>© 2026 Personalized You &nbsp;·&nbsp; <a href="/policies">Privacy & Terms</a> &nbsp;·&nbsp; <a href="/faq">FAQ</a></p>
    </footer>
    <script>
      const btn = document.getElementById('hamburger');
      const links = document.getElementById('nav-links');
      btn?.addEventListener('click', () => links?.classList.toggle('open'));
    </script>
  </body>
</html>
